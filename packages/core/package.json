{
  "private": true,
  "name": "core",
  "version": "0.1.0",
  "main": "bindings/lib.js",
  "scripts": {
    "postinstall": "yarn build:debug",
    "build:debug": "cargo build --frozen",
    "build:release": "cargo build --frozen --release",
    "clean": "cargo clean"
  },
  "dependencies": {
    "ffi": "2.2.0",
    "ref": "1.3.5"
  }
}
